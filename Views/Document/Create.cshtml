@{
    ViewData["Title"] = "Новый документ";
}
<div class="container">
    <div class="row">
        <div class="col-md-8 mx-auto">
            @await Html.PartialAsync("DocumentForm")
            @Html.ActionLink("Отмена", "Index")
            @Html.ActionLink("Отмена", "Details", new { id = Model.Id }, new { @class = "btn btn-default" })
        </div>
    </div>
</div>












